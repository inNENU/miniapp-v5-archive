module.exports = {
  getName: function (config) {
    return config.map(function (item) {
      return item.name;
    });
  },

  getNumber: function (config) {
    var num = config.toString();

    return num.length === 8 ? "0431-" + num : num;
  },
};
